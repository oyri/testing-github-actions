name: set params
on:
  workflow_dispatch:
jobs:
  variables-save:
    name: testing
    runs-on: "ubuntu-latest"
    steps:
     - name: print filename
       run: |
        touch randi.txt
        echo "filename=$(ls)" >> $GITHUB_OUTPUT
     - name: echo filename ${{ env.filename }}
       run: file ${{ env.filename }}
        
